"""[SHIM — extract post-pilot] Stand-ins for core L1/L2 (ChannelBinding,
consent, the L2 bridge + pre-speak interposition hook). Built here so the L3
build never blocks on core's reply; extracted to core C3 post-pilot.
"""
