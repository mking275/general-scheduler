[pytest]
# Feature 010 — Vera Voice test config. Repo root is on sys.path so
# `import backend.voice...` resolves. asyncio_mode=auto lets async test
# functions run without an explicit decorator.
testpaths = backend/tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
