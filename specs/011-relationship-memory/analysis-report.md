# Specification Analysis Report — 011 Relationship Memory & Consent

**Analyzer**: speckit-analyze (VetAgent quality gate) · **Date**: 2026-07-10 · **Mode**: read-only
**Constitution**: GS v1.1.0 (Platform-track exception) · **Cross-feature**: 010 (MERGED) verified against real code under `/home/matt/SMB_Hunt/General_Scheduler/backend/voice/`

## Findings

| ID | Category | Severity | Location(s) | Summary | Remediation |
|----|----------|----------|-------------|---------|-------------|
| H1 | Underspec / Inconsistency | HIGH | scoped-recall.md §C; data-model T9 `reveal_decision_log.fact_kind`; tasks T018 | **fact-kind→fact-class mapping undefined AND reveal_rules vocabulary is internally inconsistent.** Recall returns facts keyed by `fact_kind` (e.g. `identity`); policy allow-lists `fact_class` per audience. No rule maps kind→class, so the T018 evaluator has no defined input. Worse, `reveal_rules` mixes real content classes (`schedule`, `client_summary`) with **scope predicates** (`own_household_only`, `no_financial_detail`) and a wildcard (`all`) as if all were fact classes. Under structural default-deny, `client_verified: [own_household_only, no_financial_detail]` lists **no actual fact class** → a correct evaluator denies everything for verified clients. SC-001=0 rests on this evaluator. | Define an explicit `fact_kind → fact_class` taxonomy; split positive allow-classes from scope predicates into distinct policy fields applied as filters, not allow-list tokens; add task + verify. |
| H2 | Inconsistency (cross-feature) | HIGH | R5; tasks T017/T026/T030; `channel_binding_shim.py:19`; `models.py::VerificationState`; `test_foundational.py:95-111`, `test_setup.py:26` | **3→4 tier reconcile contradicts the 116-green gate and misses a second 010 enum.** Verified: 010 has TWO vocabularies — shim `VerificationLevel = none|soft_confirmed|strong` and `VerificationState = unverified|soft_confirmed` (note `none` vs `unverified` already disagree). 010 tests **hard-assert the strings** (`== "none"`, `== "soft_confirmed"`; `VerificationState.SOFT_CONFIRMED.value == "soft_confirmed"`). R5 maps `soft_confirmed→phone_match`, covers only the shim's 3 values, never mentions `VerificationState`, and drops `strong`. T017 mandates the remap; T030 mandates 116/116 green + "API unchanged" — you can't change the emitted strings without breaking those tests, and no task states the reconcile is a non-mutating translation. | Make R5 an explicit adapter that leaves 010's shim/`VerificationState` strings untouched, translating only at the core-binding boundary; enumerate ALL of 010's real values (incl. `strong`, `VerificationState`); add a preserve-strings verify. |
| H3 | Underspec (security) | HIGH | FR-018; R4; data-model T7; task T015 | **Knowledge-factor validation source & correctness unspecified.** No artifact says what the "pet's name / appointment day" answer is validated *against* or the match logic. T015's verify only checks a factor is *prompted* and failure blocks — nothing specifies/tests that a **wrong** answer fails. SC-005 ("0 changes without clearing the bar") is satisfiable by a bar that never validates. | Specify validation source per factor (pet name ← patient roster; appointment day ← 010/schedule data) + normalization/match rule; add a red-team verify (T033) that an incorrect value is rejected. |
| M1 | Ambiguity | MEDIUM | research D8; data-model Migration; T008/T034 | **Migration source-of-truth ambiguous.** T008 migrates "each `owners` row," but `owners` lives in demo SQLite; the platform is docker-compose PG with no stated platform `owners` table. T034 tests only a synthetic fixture. Real-run source (SQLite export vs PG table vs ezyVet export) undefined. | Name the concrete source system/connection for the production migration; if cross-store (SQLite→PG), say so and add the read step to T008. |
| M2 | Coverage / Measurability | MEDIUM | SC-004; T036, Pilot-Activation | **SC-004 (≥90% on "audited pilot data") is only proxy-, near-tautologically measurable here.** The real ezyVet audit is deferred; T036 measures the synthetic corpus where single-match numbers auto-ID ~100% by construction. | Reword SC-004 to name the build-time proxy, or mark the ≥90% as a Pilot-Activation gate like the ezyVet audit. |
| M3 | Inconsistency (audit semantics) | MEDIUM | scoped-recall.md §B | **ScopedRecall increments Thoth `access_count` on withheld facts.** Filtering runs *after* `recall()`, which already incremented `access_count`; a default-denied fact counts as accessed though never revealed — pollutes core telemetry that may drive sleep-agent consolidation and leaks withheld-fact access patterns into core. | Document intended semantics: push audience/scope into (future core) recall so withheld facts aren't counted, or record that shim-era access_count over-counts and must not drive salience; note in the core-rail extraction plan. |
| M4 | Terminology drift (cross-feature) | MEDIUM | data-model `contact_id`; I2 `consent_check(contact_id)` + `ConsentDecision.party_id`; `consent_shim.py` `party_id`; prefetch A4 `party_id`; T027/T028 | **party vs contact vs owner drift at the 010↔011 seam.** 010 uses `party_id`/`PartyRef`; A4's frozen field is `party_id`; 011's tables key on `contact_id`. I2 renames the param but keeps `ConsentDecision.party_id`. T027/T028 promise "shape preserved," implying an unowned `party_id⇄contact_id` translation. (Low break-risk: 010 tests call `consent_check` positionally.) | Pick one seam vocabulary, document the correspondence, and assign the translation to T027/T028. |
| M5 | Coverage gap | MEDIUM | FR-012; T017, T018-T021 | **Audience classification for owner/manager/staff has no implementing task or role source.** Tasks build only the two client-facing audiences (T021). T017 says "audience derives from tier + role" but no task defines where `role` comes from; reveal_rules define owner/manager/staff allow-lists nothing populates the audience for. | Extend T017 (or add a task) to map staff/owner/manager role→audience and name the role source (010 staff session/login role), or explicitly de-scope those audiences for 4a against FR-012. |
| M6 | Coverage gap (cross-feature) | MEDIUM | FR-023; T025/T035; 010 T033 (`consent_record`) | **FR-023 "served with disclosure" is asserted, not wired.** 010 T033 already persists a `consent_record` (disclosure text+timestamp); no 011 task emits/persists it for inbound-served opted-out callers — T035 only asserts it. | Reference 010's `consent_record` path in T025/T035 and verify an inbound-served opted-out call carries a disclosure record; or state the dependency in Assumptions. |
| G1 | Messaging Drift (Pass G) | MEDIUM | spec.md ## Marketing Output (Benefit 1 + One-Liner); plan D7 / T029 | **Benefit 1 and the one-liner claim cross-channel recognition that 4a doesn't build.** "recognized… on any channel" / "remembers your family across every channel" describe phone↔SMS↔portal↔kiosk continuity — an explicit **4b non-goal**; 4a binds `thread_id` for **single-channel voice only**. | Qualify Benefit 1 + one-liner to single-channel (voice) for 4a, or defer "every channel" to the 4b announcement, before speckit-marketing. |
| L1 | Duplication | LOW | FR-008, FR-017 | Near-duplicate on "soft-confirm never authorizes a change." | Keep FR-017 as the change-gate; narrow FR-008 to the reveal-scope clause. |
| L2 | Inconsistency (stale) | LOW | discover.md Constitution Check + surprise #5 | discover.md still frames the constitution as "v1.0.0 … superseded," predating the v1.1.0 exception the plan correctly cites. Not a violation. | Optional: annotate discover.md that supersession was formalized as the v1.1.0 Platform-track exception. |
| L3 | Terminology drift | LOW | FR-012 prose vs enum across data-model/contracts/tasks | Prose ("verified client"/"unverified caller") differs in form from enum (`client_verified`/`caller_unverified`). | Note the prose↔enum correspondence once in Key Entities. |

## Coverage verdict (FR↔task, both directions)
**Forward: 24/24 FRs have ≥1 task (100% nominal).** Three are thin/partial: FR-006 (resolve-before-first-turn only implicit), FR-012 (owner/manager/staff classification + role source unbuilt — M5), FR-023 (disclosure emission unwired — M6). **Reverse: no orphan tasks** — T001–T007 (infra), T029 (thread bind/scope-guard), T030 (regression gate) are legitimate non-FR tasks. All 7 SCs have a task; SC-004 measurability (M2) and SC-005 correctness (H3) are qualified.

## Cross-feature verification results (against real 010 code)
- **VerificationLevel**: shim is 3-tier `none|soft_confirmed|strong` (`channel_binding_shim.py:19`); a *second* enum `VerificationState = unverified|soft_confirmed` exists in `models.py`. 011/R5 quotes the shim accurately but ignores `VerificationState`, drops `strong`, and the remap collides with hard-asserting tests → **H2**.
- **ConsentDecision shape**: fields match; param renamed `party_id`→`contact_id` (positional callers safe) → **M4**.
- **HouseholdSummary A4**: all six frozen fields match `prefetch.py` exactly — accurate.
- **116-test claim (T030)**: confirmed exactly 116 `def test_` in `backend/tests/voice/` — accurate but jeopardized by H2.
- **Opt-out registry vs 010 T008/T033**: T008 semantics match 011's registry. **T033 is a different concept** (recording/no-training disclosure attestation), not opt-out; 011 leans on it for FR-023 but never wires it → **M6**.

## Pass G verdict
One MEDIUM finding (**G1**): the Feature Brief's Benefit 1 and one-liner overclaim cross-channel recognition, which is a 4b non-goal — 4a is single-channel voice only. Feature name ("The Vera Who Knows Your Family") and Benefits 2–3 (scoping / channel-aware opt-out) are consistent with what's built. Resolve G1 before speckit-marketing.

## Positive verdicts
- **Privacy invariants consistent (PASS)**: default-deny, never-enumerate-names-aloud, caller-ID-never-authorizes are stated identically across spec, discover, plan, research, all three contracts, and tasks.
- **No dependency cycles**; task graph acyclic, stated critical paths hold, no integration-before-foundation ordering violation.
- **Constitution: no violation** — 011 declares the Principle III platform-track departure explicitly and consistently under v1.1.0 (plan Constitution Check names target/clause/why).

## Metrics
FRs 24 · SCs 7 · Tasks 36 · FR coverage 24/24 nominal (3 thin) · **Findings 13 — CRITICAL 0 · HIGH 3 · MEDIUM 6 · LOW 3** · Ambiguity 2 · Duplication 1.

## Recommended remediation order
No CRITICAL — not blocked from implement, but fix the three HIGH first (they sit on the SC-001/SC-005 security boundary and the 010 regression gate):
**H1 → H3 → H2 → M5 → M1 → M6 → M4 → M3 → M2 → G1 → L1/L2/L3.**

Candidate findings (a)–(f) all confirmed and classified: (a)=H1, (b)=H3, (c)=M1, (d)=M2, (e)=H2 (010's real enum is 3-tier `none|soft_confirmed|strong` plus a second `unverified|soft_confirmed` enum — the reconcile genuinely risks the 116-green gate), (f)=M3.
